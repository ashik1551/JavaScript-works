var arr = ["hello", "good", "morning", "helloguyz"]

// var upper=arr.map(w=>w.toUpperCase())
// console.log(upper);

// var len=arr.map(w=>w.length)
// console.log(len);

// var st=arr.filter(w=>w.startsWith("hello"))
// console.log(st);

// longest=arr.reduce((w1,w2)=>w1.length>w2.length?w1:w2)       //  longest word
// console.log(longest);

// shortest=arr.reduce((w1,w2)=>w1.length<w2.length?w1:w2)         //  shortest word
// console.log(shortest);